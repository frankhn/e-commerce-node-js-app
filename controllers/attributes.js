/* eslint-disable no-unused-vars */

/**
 * attributes class
 */
class Attributes {
/**
     * @author frank
     * @param {*} req
     * @param {*} res
     * @returns {*} attribute
     */
  async signup(req, res) {
  }

  /**
     * @author frank
     * @param {*} req
     * @param {*} res
     * @returns {*} attribute
     */
  async login(req, res) {

  }

  /**
     * @author frank
     * @param {*} req
     * @param {*} res
     * @returns {*} attribute
     */
  async logout(req, res) {

  }
}

export default Attributes;
